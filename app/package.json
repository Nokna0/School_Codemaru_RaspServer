{
  "name": "square-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Square (codemaru.store) self-hosted backend + static frontend for Raspberry Pi",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js"
  },
  "overrides": {
    "fast-uri": "^3.1.2"
  },
  "dependencies": {
    "@fastify/cookie": "^9.3.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/multipart": "^8.3.0",
    "@fastify/static": "^7.0.4",
    "@fastify/websocket": "^10.0.1",
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.3.0",
    "fastify": "^4.28.1",
    "jsonwebtoken": "^9.0.2",
    "sharp": "^0.33.5",
    "zod": "^3.23.8"
  }
}
